#!/usr/bin/env bash
# Frontend shim: takes a .stream file and plays the app named inside it.
set -euo pipefail
: "${RETROPI_PREFIX:=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
file=${1:?.stream file required}
exec "$RETROPI_PREFIX/bin/retropi-stream" play "$(head -n1 "$file")"
